[project]
name = "helios-agent-fleet"
version = "0.1.0"
requires-python = ">=3.11"
description = "Bounded Hermes/XCore learning and pruning runtime"

[tool.ruff]
line-length = 120

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "basic"
